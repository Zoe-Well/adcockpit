# AdCockpit v2.0 — FastAPI Backend Dependencies

# Core
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0

# LLM / Agent
openai>=1.0
langgraph>=0.2
langchain>=0.2
langchain-openai>=0.1

# HTTP & Feishu
httpx>=0.27
requests>=2.28
python-dotenv>=1.0
sse-starlette>=2.0

# Async (optional — code ready, not activated yet)
# celery>=5.3
# redis>=5.0

# Vector Search (optional — code ready, not activated yet)
# chromadb>=0.4
