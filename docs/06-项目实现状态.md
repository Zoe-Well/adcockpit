# AdCockpit v2.0 — 项目实现状态

**更新日期**: 2026-06-17 (持续更新)

---

## 一、架构概览

```
React 18 + Tailwind CSS + shadcn/ui (5173)  ←→  FastAPI (8000)  ←→  Mock Functions + Feishu Client
                                                                  ←→  Local JSON (backend/data/)
```

| 层 | 技术 | 状态 |
|----|------|:--:|
| 前端 | React 18 + TypeScript + Tailwind CSS + shadcn/ui 组件 | ✅ |
| 后端 | FastAPI + Pydantic | ✅ |
| Agent 编排 | LangGraph + DeepSeek 节点 (Python 侧 API 连接异常) | ⚠️ |
| Agent 输出 | 6 节点丰富思考过程 (多行详细输出 → 前端 Trace Board 展开/收起) | ✅ |
| 工具层 | 24 Mock Functions + Feishu Client | ✅ |
| 数据持久化 | Local JSON files (backend/data/) | ✅ |
| 异步任务 | Celery + Redis (代码就绪) | ⏳ |
| 向量检索 | Chroma + RAG (代码就绪) | ⏳ |
| 流式推送 | WebSocket endpoint (代码就绪) | ⏳ |
| Streamlit v1 | `ui/` 完整保留, 独立可用 | ✅ |

---

## 二、用户故事总览

| 用户故事 | 完整度 | 核心差距 |
|---------|:--:|------|
| US1 投放优化 | **100%** | — |
| US2 内容生产 | **100%** | — |
| US6 广告投放 | **95%** | 错误提示待增强 |
| US3 电商场控 | **15%** | 标签页框架 + 意图识别, 无业务交互 |
| US4 数据分析 | **15%** | 标签页框架 + 意图识别, 无业务交互 |
| US5 故障诊断 | **15%** | 标签页框架 + 意图识别, 无业务交互 |

---

## 三、用户故事详细状态

### US1 — 跨平台广告投放智能优化 (P1) 🎯 已完成的 MVP

| 功能 | 状态 |
|------|:--:|
| 对话输入 → 意图识别 → 优化参数卡片 | ✅ |
| 参数自定义（平台/天数/阈值/出价/预算） | ✅ |
| Trace Board 7 节点流式动画 + 丰富思考过程展示 | ✅ |
| 每个节点卡片展示多行详细输出（意图分析/数据明细/异常逐条/策略方案/执行进度） | ✅ |
| 输出超过 3 行自动折叠，支持「展开全部 / 收起」 | ✅ |
| 动画停在"执行操作 — 等待确认"，报告节点灰色等待 | ✅ |
| 审批卡片弹出（确认执行 / 取消） | ✅ |
| 确认后执行卡片展示逐项操作进度 + 结果 | ✅ |
| 确认后报告卡片展示调整摘要 + 节省预估 + 后续建议 | ✅ |
| Dashboard 指标卡 + ROI 柱状图（阈值虚线）+ 告警 | ✅ |
| 分页投放计划表格（5 条/页，上/下翻页） | ✅ |
| 优化后数据持久化到 `backend/data/plans_*.json` | ✅ |
| 优化历史记录存入 `optimization_history.json` | ✅ |

**US1 完整度: 100%**

---

### US2 — 投后素材分析与 AI 内容工厂 (P2)

| 功能 | 状态 |
|------|:--:|
| 对话输入"素材/脚本" → 意图识别 → 内容生产参数卡片 | ✅ |
| 参数自定义（平台/模板/数量） | ✅ |
| Trace Board 6 节点流式动画 + 丰富思考过程展示 | ✅ |
| 调用 `/api/agent/content` → LangGraph Agent 编排 → 返回真实步骤 | ✅ |
| Supervisor 节点：置信度 0.91 + 场景匹配 + 5步执行计划 | ✅ |
| Data 节点：高CTR 3条(5.7%) vs 低CTR 3条(1.5%) 逐条对比 + 定量差距 | ✅ |
| Analysis 节点：4项爆款特征(含↑倍数) + 3项低点击通病(含流失率) | ✅ |
| Content 节点：每条脚本4段结构拆解 + 模板复用标注 + CTR预估 | ✅ |
| Execute 节点：等待确认 → 确认后逐项执行(生成→发布→保存模板) | ✅ |
| Report 节点：产出摘要 + CTR预估 + A/B测试建议 | ✅ |
| 每个节点卡片多行输出，超过 3 行自动折叠 + 展开/收起 | ✅ |
| 审批卡片动态显示模板名+数量 | ✅ |
| 确认后执行+报告卡片展示富文本输出 | ✅ |
| 脚本发布到飞书并返回文档链接 | ✅ |
| Dashboard 专属内容生产视图（指标+特征分析+记录表） | ✅ |

**US2 完整度: 100%** (全链路 LangGraph Agent 编排 + 丰富思考过程 + 展开/收起 UI)

---

### US6 — 广告投放计划创建与管理 (P2)

| 功能 | 状态 |
|------|:--:|
| 侧边栏「广告投放」标签页 | ✅ |
| 投放计划表单（平台/名称/预算/出价/定向） | ✅ |
| 参数校验（预算>=500、出价 5-200） | ✅ |
| Trace Board 4 节点流式动画 + 丰富思考过程展示 | ✅ |
| 调用 `/api/agent/create` → LangGraph Agent 编排 → 返回真实步骤 | ✅ |
| Supervisor 节点：5项参数逐项校验(平台/名称/预算区间/出价区间/定向) | ✅ |
| Data 节点：5项审核逐条通过 + 耗时 0.8s + 账户余额检查 | ✅ |
| Execute 节点：等待确认 → 确认后展示实时创建进度 + 预估曝光 | ✅ |
| Report 节点：状态 active + 首日消耗预估 + 运营建议 | ✅ |
| 每个节点卡片多行输出，超过 3 行自动折叠 + 展开/收起 | ✅ |
| 审批卡片（确认执行/取消） | ✅ |
| 确认后创建计划并调用 `/api/campaigns/create` | ✅ |
| 创建后计划出现在 Dashboard 表格 + 创建记录列表 | ✅ |
| Dashboard 专属投放视图（指标+创建记录+平台分布） | ✅ |
| 表单错误提示 | ⚠️ 简化为 st.error |

**US6 完整度: 95%** (全链路 LangGraph Agent 编排 + 丰富思考过程，仅错误提示待增强)

---

### US3 — 直播间实时 AI 场控与电商闭环 (P2)

| 功能 | 状态 |
|------|:--:|
| 直播监控标签页 | ✅ |
| 意图识别支持直播/库存关键词 | ✅ |
| 库存查询 + 补货交互 | ❌ |
| 优惠券创建 | ❌ |
| 催单话术生成 | ❌ |

**US3 完整度: 15%** (标签页框架 + 意图识别)

---

### US4 — 跨渠道数据分析与预算决策 (P2)

| 功能 | 状态 |
|------|:--:|
| 数据分析标签页 | ✅ |
| 意图识别支持报表/分析关键词 | ✅ |
| 多平台数据聚合 + 客户排名 | ❌ |
| 预算建议 + PPT 提纲 | ❌ |

**US4 完整度: 15%** (标签页框架 + 意图识别)

---

### US5 — 全链路故障诊断与自主恢复 (P3)

| 功能 | 状态 |
|------|:--:|
| 故障诊断标签页 | ✅ |
| 意图识别支持诊断/排查关键词 | ✅ |
| 计划状态查询 + 根因分析 | ❌ |
| 自动恢复交互 | ❌ |

**US5 完整度: 15%** (标签页框架 + 意图识别)

---

## 四、Trace Board 场景对照

| 场景 | 节点数 | 各节点详细输出内容 |
|------|--------|-------------------|
| 🎯 投放优化 | 7 节点 | ① Supervisor: 置信度0.94 + 场景匹配 + 5步执行计划列表 → ② Data·抖音: 5条计划逐条(ID/消耗/ROI/出价) + 总消耗¥60,500 → ③ Data·腾讯: 5条计划逐条 + 合并 → ④ Analysis: 扫描10条 + 异常4条(40%) + 严重2条 + 逐条标注🔴🟡 → ⑤ Strategy: 调价模型公式 + 4条方案逐条(ID/降幅/原因) + 预估日省¥6,400 → ⑥ Execute: 待审批 3条计划 4项调整 → ⑦ Report: 调整摘要 + 节省预估 + 后续建议 |
| 🎬 内容生产 | 6 节点 | ① Supervisor: 置信度0.91 + 场景匹配 + 5步拆解 → ② Data: 高CTR 3条(均值5.7%) vs 低CTR 3条(均值1.5%) 逐条对比 → ③ Analysis: 4项爆款特征(含定量数据)+3项低点击通病 → ④ Content: 每条脚本4段结构拆解 + 模板复用 + CTR预估↑40% → ⑤ Execute: 3条脚本待发布 + 3项后续操作 → ⑥ Report: 产出摘要 + CTR预估 + A/B测试建议 |
| 📊 新建计划 | 4 节点 | ① Supervisor: 5项参数逐项校验(平台/名称/预算/出价/定向) → ② Data: 4项审核逐条通过 + 耗时0.8s → ③ Execute: 待上线 + 日预算¥5,000 + 预估曝光8K-12K → ④ Report: 状态active + 首日消耗预估 + 运营建议 |

### 丰富输出生效范围

| 场景 | Agent API | `agentSteps` 展开/收起 UI | 实际效果 |
|------|:--:|:--:|------|
| 🎯 投放优化 (US1) | `/api/agent/optimize` | ✅ 已接入 | 全链路 LangGraph 编排 → 6 节点丰富输出 → 折叠/展开 |
| 🎬 内容生产 (US2) | `/api/agent/content` | ✅ 已接入 | 全链路 Agent 编排 → 6 节点丰富输出 → 折叠/展开 |
| 📊 新建计划 (US6) | `/api/agent/create` | ✅ 已接入 | 全链路 Agent 编排 → 4 节点丰富输出 → 折叠/展开 |

> **2026-06-16 更新**：三个场景全部实现 LangGraph Agent 编排 + 丰富思考过程输出。后端 `agent.py` 新增 `/api/agent/content` 和 `/api/agent/create` 端点，各含专用节点输出构建函数。前端 `runContent()`/`runCreate()` 已从 `traceStep` 老动画路径迁移到 `agentSteps` API 驱动渲染链路。确认按钮 (`confirmContent`/`confirmCreate`) 同样展示富文本执行和报告输出。

---

## 五、前后端 API 对接状态

| API 端点 | 前端调用位置 |
|---------|------------|
| `POST /api/intent/classify` | `sendMsg()` — 对话意图识别 (LLM + keyword fallback) |
| `GET /api/campaigns/all` | `loadPlans()` — Dashboard 数据加载 |
| `POST /api/agent/optimize` | `runOptimize()` — 投放优化 Agent 编排 (LangGraph) |
| `POST /api/agent/content` | `runContent()` — 内容生产 Agent 编排 |
| `POST /api/agent/create` | `runCreate()` — 计划创建 Agent 编排 |
| `POST /api/campaigns/optimize` | `confirmOptimize()` — 确认后执行优化 |
| `POST /api/campaigns/create` | `confirmCreate()` — 确认后创建计划 |
| `POST /api/content/generate` | `confirmContent()` — 确认后生成脚本 |
| `POST /api/content/publish` | `confirmContent()` — 发布到飞书 |
| `GET /ws/{session_id}` | 待前端集成 |

---

## 六、数据持久化

| 文件 | 内容 | 写入时机 |
|------|------|---------|
| `backend/data/plans_douyin.json` | 抖音计划数据 | 优化后自动更新 |
| `backend/data/plans_tencent.json` | 腾讯计划数据 | 优化后自动更新 |
| `backend/data/optimization_history.json` | 优化记录 | 每次优化后 |
| `backend/data/content_records.json` | 内容生产记录 | 内容生成后 |
| `backend/data/feishu_urls.json` | 飞书文档链接 | 发布后 |

---

## 七、已知问题

| # | 问题 | 优先级 | 详情 |
|---|------|--------|------|
| 1 | DeepSeek LLM Python 侧连接失败 | P0 | `curl` 可达且 key 有效 (401→200)，Python `openai` 库抛 `Connection error`，怀疑代理/SSL/Windows 网络层问题。意图分类触发 3 次重试后走 keyword fallback。 |
| 2 | WebSocket 未在前端连接 | P1 | endpoint 已就绪，前端 `App.tsx` 未集成 WS 客户端 |
| 3 | US3 仅有标签页框架, 无业务交互 | P2 | — |
| 4 | US4 仅有标签页框架, 无业务交互 | P2 | — |
| 5 | US5 仅有标签页框架, 无业务交互 | P3 | — |
| 6 | Redis + Chroma 代码就绪但未激活 | P2 | — |
| 7 | Streamlit v1 和 React v2 并存, 入口不统一 | P3 | — |
